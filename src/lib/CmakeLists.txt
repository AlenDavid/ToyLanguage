# lib library

add_subdirectory(syntactic_analysis)
add_subdirectory(input_parser)
add_subdirectory(module_emmiter)
add_subdirectory(lexical)
add_subdirectory(tokens)

add_library(Libs
  input_parser/input_parser.cpp
)

target_compile_definitions(Libs PUBLIC Libs)

target_link_libraries(Libs
  ${LLVM_LIBS}
  SyntacticAnalysis
  InputParser
  ModuleEmmiter
  Lexical
  Tokens
)
